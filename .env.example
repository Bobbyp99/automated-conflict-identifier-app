POSTGRES_USER=app_user
POSTGRES_PASSWORD=change
POSTGRES_DB=conflict_database
POSTGRES_PORT=5432
POSTGRES_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}
